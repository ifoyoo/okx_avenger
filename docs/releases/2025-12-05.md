# 2025.12.05 「继敏」发布说明

## 新增
- LLM 提示词引入“资金与风控、持仓、近期 P/L”三段上下文，让 AI 决策基于真实持仓与账户表现。
- 主线程预读取持仓与绩效快照，逐个传入策略执行与 LLM 解析，保证各合约都能按照独立仓位分析。

## 改进
- Watchlist 运行时记录账户持仓，Log/通知可追踪触发时的仓位风险。
- 默认版本号更新至 `2025.12.05 「继敏」`，便于区分新旧构建。

## 兼容性
- 无需数据迁移；请确保更新 `.env` 的 `APP_VERSION` 字段，并重新部署以载入新的提示词逻辑。
